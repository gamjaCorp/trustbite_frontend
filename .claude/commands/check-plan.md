---
description: 직전 작업 내용을 docs/plan/roadmap1/week-*.md 체크리스트에 반영한다
---

이 대화에서 방금 완료한 작업을 `docs/plan/roadmap1/week-*.md` 체크리스트에 반영한다.
사용자가 추가 힌트를 전달했으면 `$ARGUMENTS` 에 있으니 매칭 시 우선 참고한다.

## 1단계 — 직전 작업 식별

대화 컨텍스트에서 아래 정보를 추출한다.

- 이번 대화에서 생성/수정한 파일 경로
- 추가한 컴포넌트·훅·API 함수명
- 완료 처리된 shrimp-task-manager 태스크 이름
- 기타 사용자가 "완료"·"끝남"·"됐다"고 언급한 작업

위 목록이 비어 있으면 "이번 대화에서 식별된 완료 작업이 없습니다." 를 출력하고 종료한다.

## 2단계 — 주차 파일 매칭

아래 파일을 모두 Read 한다.

```
docs/plan/roadmap1/week-0.md
docs/plan/roadmap1/week-1.md
docs/plan/roadmap1/week-2.md
docs/plan/roadmap1/week-3.md
docs/plan/roadmap1/week-4.md
```

각 파일에서 **미체크 항목**만 후보로 추린다.

- `- [ ]` 로 시작하는 체크박스 라인
- 일별 요약 표의 ` ☐ |` 로 끝나는 셀
- `## Day N ...` 헤더 안의 `☐`
- `# Week N ...` 헤더 안의 `☐`

1단계에서 추출한 파일 경로·컴포넌트명·함수명·태스크 이름을 키워드로 삼아 후보 라인과 매칭한다.
확신이 낮은 항목은 제외한다 — **잘못 체크하는 것보다 빠뜨리는 게 낫다.**

## 3단계 — Preview 출력 (Edit 호출 금지)

매칭 결과를 아래 표 형태로 출력한다.

| 파일 | 기존 텍스트 (라인 앞 20자) | 변경 후 | 근거 |
|------|--------------------------|---------|------|

종속 갱신도 같은 표에 포함한다.

- 어떤 Day 의 모든 `- [ ]` 가 체크되면 → 해당 Day 헤더의 `☐` 와 일별 요약 표 해당 행의 `☐` 도 `☑` 로 추가 등재
- 어떤 주차의 모든 Day 헤더가 `☑` 가 되면 → 파일 상단 `# Week N ... ☐` 도 `☑` 로 추가 등재

매칭이 0건이면 "매칭된 항목이 없습니다. `$ARGUMENTS` 에 작업 키워드를 직접 적어 다시 시도해 보세요." 를 출력하고 종료한다.

## 4단계 — 승인 대기

```
위 변경을 적용할까요? (y / 제외할 항목 지시)
```

한 줄만 출력하고 대기한다. 사용자가 일부 제외를 요청하면 표를 수정해 재출력하고 다시 대기한다.

## 5단계 — 적용

승인 후 각 매칭 항목에 대해 Edit 으로 치환한다.

치환 규칙:
- `- [ ]` → `- [x]`
- ` ☐ |` → ` ☑ |` (표 셀)
- 헤더 끝 ` ☐` → ` ☑`

모든 Edit 완료 후 아래 형식으로만 출력한다.

```
체크 완료: N건
- docs/plan/roadmap1/week-X.md
```

## 가드레일

- `docs/plan/roadmap1/week-*.md` 외의 파일은 절대 수정하지 않는다.
- git 커밋, 패키지 설치, 다른 슬래시 커맨드 호출 금지.
- 매칭 확신도가 낮으면 preview 단계에서 "후보 부족 — 수동 지정 필요" 라고 표시하고 체크하지 않는다.
