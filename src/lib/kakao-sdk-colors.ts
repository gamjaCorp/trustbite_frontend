/**
 * Kakao Maps SDK에 전달하는 raw 색상 상수 모음.
 * SDK API가 CSS 변수를 받지 못해 hex 리터럴이 불가피함 — 이 파일을 단일 출처로 관리한다.
 * 디자인 토큰(globals.css)이 변경될 경우 이 파일도 함께 수정할 것.
 */

// 검색 반경 원 채움색 — globals.css --color-primary(oklch 기반)의 hex 근사값
export const SDK_PRIMARY_COLOR = '#ff7a00';
